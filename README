## Run the program
Download the EncodingDecoding.jar and run in your terminal:

```bash
java -jar EncodingDecoding.jar


Note - make sure you are in the directory where you downloaded EncodingDecoding.jar, usually (cd download)
